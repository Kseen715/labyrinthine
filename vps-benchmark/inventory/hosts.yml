---
# Default inventory — runs the benchmark against localhost.
# Replace or extend with real VPS hosts as needed:
#
#   all:
#     hosts:
#       my-vps-1:
#         ansible_host: 1.2.3.4
#         ansible_user: root

all:
  hosts:
    localhost:
      ansible_connection: local
